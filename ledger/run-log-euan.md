# 编排并行 Run Log

> 每次 orchestrated-parallel run 合流时由主会话追加一条。模板在文末。
> 目的:为 agent-on 积累真实数据——并行 vs 串行的时间账、冲突率、契约悬点命中率。

## Run #1 — S2 灵魂闭环(2026-07-03)

- **轨道**:🔧 后端(captures/extraction/reviews)× 🎨 前端(compose/reviews/confirm UI)
- **时长**:后端 21min / 前端 53min(墙钟=53min,串行估 74min+)
- **冲突**:0(轨道=目录切分干净)
- **契约悬点**:0——fixtures 先冻结,双端各自消费同一文件
- **返工**:0
- **教训**:前端轨发现 whenComplete 死锁(表达式体回调等自己)——记入 loop-notes

## Run #2 — S3 客户切片(2026-07-03)

- **轨道**:🔧 s3.1 customers 后端 × 🎨 s3.2 客户 UI
- **冲突**:0
- **契约悬点**:1 个(前端轨怀疑 PATCH 响应形状)→ 合流时验证后端恰好返回 CustomerDetail,**零改动闭合**——契约先行的价值实证
- **返工**:0
- **教训**:悬点不阻塞轨道:前端按假设(fixtures)继续,合流时统一裁决

## Run #3 — S4 今日+任务(2026-07-03)

- **轨道**:🔧 s4.1 today/tasks 后端+模块④ × 🎨 s4.2 今日 UI+任务+本地通知排程器
- **时长**:后端 17min / 前端 29min(墙钟=29min,串行估 46min+)
- **冲突**:0(轨道=目录切分,第三次验证)
- **契约悬点**:9 个(后端 5 + 前端 4),全部合流时裁决——**1 个真撞车**:fixture tasks_due 排序与卡片规则矛盾,后端按卡片实现、前端按 fixture 顺序断言;裁决=fixture 跟正(due_at asc),改前端断言 1 处
- **返工**:2 处小修(上述断言 + login_flow_test 未钉新 Fake:合流翻 Dio 后进壳测试裸奔——**教训:任何"进壳"的测试都要钉全量 Fake overrides,不能只钉 auth**)
- **教训**:fixture 既锁形状也锁顺序语义——冻结时就该按实现规则排好,别让两轨各自解读(已在 loop-notes 展开)

## Run #4 — S5 账户+周报(2026-07-03)

- **轨道**:🔧 s5.1 注册/限流/me/周报后端+migration × 🎨 s5.2 注册/忘记/我的/周报 UI
- **时长**:后端 35min / 前端 25min(墙钟=35min,串行估 60min)
- **冲突**:0(第四次验证;后端轨界扩到 supabase/migrations 也没撞)
- **契约悬点**:13 个(后端 7 + 前端 6),合流核对**全部相容零返工**——display_name null:后端说会给 null,前端恰好建模成 String?;文案透传:前端锁 fixture 文案,后端恰好透传同文案。Run #3 的 fixture 教训(冻结时把语义写死)见效
- **返工**:0;翻 Dio 一次绿(Run #3 的"全量钉 Fake"教训生效)
- **教训**:后端轨 LIVE 并行跑出 GoTrue global-logout 401 真分支(原会 500)——**并行 LIVE 本身是发现器**,不只是验证器

## Run #5 — S5b 计费地基(2026-07-04)

- **轨道**:🔧 s5b.1 subscriptions/webhook/计量/双层配额+migration × 🎨 s5b.2 订阅区块/超限引导/UrlOpener
- **时长**:两轨各被 API 断连打断一次(~10min 处),SendMessage 原地续跑;总墙钟 ≈33min(含断点)
- **冲突**:0(第五次验证)
- **契约悬点**:4 条(past_due 派生/两字段错误形状/quota.limit 口径/动态 url),合流对账**全相容零返工**
- **返工**:0;翻 Dio 一次绿
- **教训**:①**断点续跑实证**:worktree(文件态在盘上)+ 自包含 phase 卡(不依赖会话记忆)= 断连后一条消息原地满血复活,这是"自包含文件系统"设计的直接回报;②外部依赖缺位时给 AI「⏸ 挂账+事后验证步骤」的出路,报告诚实度 100%(Stripe LIVE 无一句伪造)

## Run #6 — S6.1 迁移 × S7.1 QA 清账(2026-07-04,异构双轨)

- **轨道**:📦 migration/(全新目录)× 🧹 api/(测试为主+1 处生产)——第一次非「前后端」形态的并行,轨道=目录原则照样成立
- **时长**:迁移 28min / 清账 17min(墙钟=28min)
- **冲突**:0(第六次验证)
- **契约悬点**:1 个技术偏差主动报备(partial→全量唯一索引,PostgREST on_conflict 不认 partial)——**「偏差报备」比「静默变通」值钱**,进 SOP
- **返工**:0
- **教训**:①LIVE 是发现器再证:微秒窗坑只有真库能暴露;②「三层护栏」模式(只读事务+SQL 白名单+静态扫描断言)适合一切「绝不能写」的场景,可复用
- **同日主会话侧**:网关生产部署踩掉 @vercel/node ESM specifier 坑(84 处补 .js)——部署冒烟要趁早

## Run #7 — S9 性能切片(2026-07-04)

- **轨道**:🔧 网关(度量/ETag/签名memo/limit)× 🎨 客户端(SWR/骨架/预取/弱网)
- **时长**:网关 22min / 客户端 48min(墙钟=48min)
- **冲突**:0(第七次验证)
- **契约悬点**:8(5+3),全裁决;唯一口径分歧「today 要不要 limit」两轨各自独立做出了相同判断(聚合端点不带)——契约语义清晰时,双轨会自然收敛
- **返工**:0;canary 两轮修的是**平台行为**(Vercel 剥头/304 剥光),不是轨道错误
- **战果数字**:regions=sin1 一行 → /reviews 289→42ms(7×);SWR 上机=二次进页零转圈
- **教训**:①「度量先行」当天回本——Server-Timing 上线 10 分钟就定位了地域错配;②主会话在合流后做的生产 canary 是双轨模式的必要第三环:轨道各自绿 ≠ 边缘出口绿

## Run #8 — S10.1 数据对称后端 × 官网视觉稿(2026-07-05,单 Ship 轨+Explore 轨)

- **轨道**:🔧 api/(S10.1)× 🏎 website/explore(官网首页稿)——三人类会话并行期,轨道=目录纪律首次跨「人机混合」场景(🅾 麦克风窗口/🆄 UI 窗口/🅼 主会话各占目录)
- **时长**:api 轨 29min;Explore 轨并行
- **冲突**:0(第八次验证,含三人类会话在场)
- **悬点**:4 个 Error Signal 全部上报零自行定论(旧 token 窗口/Stripe past_due/清号回放/第五读面)——裁决:接受/T23/铁证足/qa#12
- **返工**:0
- **教训**:①「绝对保护」条款(DEV 账号 403)是危险端点的必备件——删除类 API 先想「什么绝不能删」再想「删什么」;②审计类操作(清号)的 console 证据会被输出链吞,**终态核查脚本**(残留 NONE+DEV intact)比过程日志可靠

## Run #9 — S12.1 评测集+小修 × 法务文本(2026-07-06)

- **轨道**:🔬 api(eval harness+qa#12+T23)× 📜 website/legal(六文件)
- **冲突**:0(第九次;四会话期)
- **教训**:①**金标池 n=1 的诚实**:评测集的瓶颈不是工具是数据——「确认闭环白送评测资产」要等产品被真用才兑现;先建 harness 立口径是对的,基线告警写明 n=1 不支撑调优;②法务域同样适用「验证后才说完成」:未核实的条款不承诺(training/静态加密/会话失效),政策与代码状态咬合埋同步钩子

---

## 模板

```
## Run #N — <切片名>(YYYY-MM-DD)
- **轨道**:
- **时长**:各轨 / 墙钟 / 串行估
- **冲突**:N(哪个文件)
- **契约悬点**:N(什么悬点,怎么闭合)
- **返工**:N(合流后改了什么)
- **教训**:一行,展开的进 loop-notes
```
