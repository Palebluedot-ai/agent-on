# intake — IPONews 第三次结账(2026-07-15)

> 来源会话:复盘「二次结账回执不卫生」——不是结账漏写,是回执 commit 困在 feature/worktree 未进 main + 待消化 N 无反向清零。用户拍板两道防呆入协议。项目 pin v0.3.0。
> 附:同批回填 07-13 二次结账回执(acf6e4a 仅在旁支,main 此前漏记)。

### settlement-receipt-on-default-branch(结账回执必须落在项目 default branch)
- source:IPONews @ 4b0f686 | pin v0.3.0
- evidence:二次结账回执 commit `acf6e4a`（message:「agent-on 结账回执:第二次出仓 4 卡,lock+loop-notes 更新」）存在且 diff 正确(lock 追加 07-13 行 + loop-notes 待办位改 4);但 `git merge-base --is-ancestor acf6e4a HEAD` 在 main 上为假,`git branch --contains acf6e4a` 仅 `claude/rag-architecture-planning-5ab68f`。同期 agent-on intake `2026-07-13-IPONews.md` 已收件并第七次消化全 landed——跨仓两侧耐久性不对称:方法论进真线、项目账本困旁支。
- confidence:high(单项目但 git 可回放,因果闭合)
- claim:结账 step5 回执(lock last_settlement + loop-notes 待办位)必须落在项目 default branch(通常 main);若会话在 worktree/feature 上,只允许写 agent-on intake,项目回执须 checkout main 或 cherry-pick 后再报完成;验收=`git merge-base --is-ancestor <回执commit> origin/main`(或本地 main)为真,否则不算结账完成。
- suggested_landing:boot/settlement.md 上半场 step5 加「default-branch 验收」硬门 + session-handshake 或 merge-checklist 合流时巡检「结账回执类 commit 是否困在死枝」(与 worktree-sprawl 同族)
- rollback:revert 落地 commit
- trace:IPONews acf6e4a; agent-on 6e3cf1f + 第七次消化 landed 批;本会话 git 取证 2026-07-15
- 状态:landed@同批(第九次消化:settlement 上半场 step5 加 default-branch 硬门 + session-handshake 读取表「工作树蔓延」行扩结账回执困死枝巡检)

### pending-digest-reconcile-on-read(待消化 N 读时对账,消化端不回写项目)
- source:IPONews @ 4b0f686 | pin v0.3.0
- evidence:settlement 上半场 step6 只规定结账时写「待消化 N」;消化在 agent-on 仓会话完成且跨仓边界禁止回写项目 → 无「清 0」责任人。IPONews main 上 loop-notes 顶部长期保留「待消化 3 张」(07-12 intake),而该文件与 07-13 四卡均已 landed——handshake 读表会把过期 N 当真。acf6e4a 旁支版曾把 N 改为 4,即使合入 main 也会在消化后粘住。
- confidence:high(协议缺口 + 现场假象并存)
- claim:握手或结账 step0 读到「待消化 N」时,打开对应 intake 文件;若全部卡已 landed/rejected/deferred,当场把待办位改 N=0 并随项目仓 commit——清 0 责任在「下次打开项目」的会话,不在消化端。
- suggested_landing:boot/session-handshake.md 读取表「有没有卡等着消化」行扩为对账动作 + boot/settlement.md 上半场 step0/step6 交叉引用(结账幂等开场先清已收口积压)
- rollback:revert 落地 commit
- trace:IPONews loop-notes 顶部待办位; intake/2026-07-12-IPONews.md 与 2026-07-13-IPONews.md 全 landed; settlement.md step6 与跨仓零 git 边界
- 状态:landed@同批(第九次消化:session-handshake 读取表「待消化」行扩读时对账 + settlement 上半场 step0 顺手对账 / step6 N 生命周期交叉引用)
