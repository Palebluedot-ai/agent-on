# intake — IPONews 第四次结账(2026-07-16)

> 来源会话:主线 phase-p-rag-grounded Loop0–6 落地 + 人审 Excel 撤出全局瓶颈。项目 pin v0.4.0。
> 域判据:只出编排/纪律卡;grounded-answer 业务实现与 NLR 探针对账留项目端,不出仓。

### long-task-loop-ledger-on-phase(跨会话长主线:phase 卡内 append-only Loop 台账)
- source:IPONews @ e5ea398 | pin v0.4.0
- evidence:phase 卡 `docs/phases/phase-p-rag-grounded.md` 开篇「长任务 Loop 协议」+ Loop 台账 0–6 行(目标/验收命令/结果);同日 commit 链 `e055643`(Loop0–1 retrieve)→`e5255ee`(Loop2 sourcePages)→`cd242db`(Loop3 cache)→`5dfcf5a`(Loop5 ask)→`e5ea398`(Loop6 MCP);`loop-notes.md` 07-16 连续五行各记一 Loop 断点;`progress.yaml` next 始终指向台账下一子步。跨 compaction/换会话可只读台账最后一行续跑,不依赖会话记忆。
- confidence:high(单项目但可回放整条 commit 链+台账闭合,且与 self_contained phase 卡同构)
- claim:跨会话长主线(一天多 Loop / 多日同一 phase)必须在 phase 卡内建 **append-only Loop 台账**(日期·目标·验收命令·结果);新会话读 progress.next + 台账最后一行断点续跑;单 Loop ≤ 可验证时间盒(默认对齐 max_feedback_loop_min);业务代码不回流 agent-on,仅可复用编排教训才结账。
- suggested_landing:kit/phase-card-template.md 增「可选·长任务 Loop 台账」节 + playbook/anti-hallucination 机制 5(自包含 phase 卡)或 mechanisms/self-contained-file-system 补半句「长 phase 内用 Loop 台账做微断点」
- rollback:revert 落地 commit
- trace:IPONews phase-p-rag-grounded.md L14–34; commits e055643..e5ea398; loop-notes 2026-07-16 Loop 五行
- 状态:landed@同批(第十一次消化:kit/phase-card-template §2b Loop 台账 + anti-hallucination 机制5 半句)

### human-async-not-global-bottleneck(人异步事项不得独占 current_bottleneck)
- source:IPONews @ 1127319 | pin v0.4.0
- evidence:升级前 `progress.yaml` 把「等用户审 518 行 Excel」写进 `current_bottleneck`,会话读表后默认卡死不推进其他轨。用户拍板后 commit `1127319` 将 excel 降为 `p-rules-v2` 并行 todo、bottleneck 改「无全局阻塞」、notes 写明「excel 审阅永不写进 current_bottleneck」;同日主线立刻开跑 RAG Loop0–6(e055643..e5ea398)。对照:域名部署仍可 `blocked` 在单 phase,但不占全局 bottleneck。
- confidence:high(before/after 同一文件可 diff;因果=瓶颈字段改写后同日多 Loop 交付)
- claim:`progress.current_bottleneck` 只登记**会挡住 agent 动手**的阻塞;人审阅/人买域名/人给密钥等 **human-async** 一律进 phases 并行 todo 或单卡 blocked,禁止单独写成全局 bottleneck 冻结整仓节奏。
- suggested_landing:kit/progress-template.yaml 在 current_bottleneck 行旁注 human-async 禁令 + playbook/mechanisms/three-stabilizers 或 monitoring-and-error-signal 补一句口径
- rollback:revert 落地 commit
- trace:IPONews 1127319; progress.yaml notes 并行纪律①; loop-notes 2026-07-16 优先级重排行
- 状态:landed@同批(第十一次消化:kit/progress-template current_bottleneck 旁注 + three-stabilizers TOC 口径)
