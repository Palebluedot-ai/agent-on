# intake — IPONews 第五次结账(2026-07-18)

> 来源会话:Application Proof 增量刷新 + 章节化 prompts + 金融级 agent 门禁;用户明确生产形态= Vercel/CF 上 LLM→API,不靠 IDE hook。  
> 项目 pin v0.4.0(落后 agent-on 最新 tag v0.5.1,含 minor 0.5.0 + patch 0.5.1;另可说「agent-on 升级」)。  
> 域判据:只出 AI 协作/纪律/防幻觉分层;HKEX 字段语义、NLR 年目录、抽数字段 schema 留项目端。

### hook-dev-floor-api-prod-gate(开发用 hook;生产防幻觉在程序 API)
- source:IPONews @ 9f1514a | pin v0.4.0
- evidence:用户结账前拍板:「服务在 vercel 或 cloudflare,不会装 Claude Code 或 Grok,靠程序不是 hook;形态是 LLM 连 API 调取信息」。同 commit 落盘 `docs/specs/agent-execution-gates.md` §0/§1 分层 + 结账说明「Hook=开发期防失手;API 门禁=生产期防幻觉进金融结论」;仓库已有 `qa.ts`/`retrieve`/`grounded-answer` refuse 与 MCP 结构化工具,准备迁 Worker 时同查询层。
- confidence:high(用户显式生产约束 + 同仓已实现双层:dev guard 与 runtime refuse)
- claim:IDE hook / session preflight 只是**开发期地板**,防 coding agent 未读规则就 push/批跑;产品一旦是「LLM → 你的 API」部署在无 agent 运行时的云上,防幻觉必须落在**查询/API 程序层**(无 Evidence 则 refused、结构化字段只读 extractions、禁止裸 chat 当事实源),不得把 hook 当生产护栏。
- suggested_landing:playbook/anti-hallucination.md 或 mechanisms 增「dev floor vs prod API gate」半节 + kit/claude-hooks-template.md 文首一句「hook 非生产」;可选 bench 案例 hook-not-prod-gate
- rollback:revert 落地 commit
- trace:IPONews 9f1514a; docs/specs/agent-execution-gates.md; 会话用户句「vercel/cloudflare…靠程序不是 hook」
- 状态:landed@同批(第十一·卡3:anti-hallucination 附节 dev floor vs prod API + claude-hooks-template 文首)

### session-receipt-before-gated-actions(高风险动作前机械会话回执)
- source:IPONews @ 9f1514a | pin v0.4.0
- evidence:`scripts/guard/preflight.mjs` 指纹 `AGENTS.md`+`docs/STATUS.json`+`docs/state/progress.yaml` 写 `.agent/session-receipt.json` 并 stdout 注入硬约束摘要;`pretool-bash.mjs` 无有效回执 exit 2 拦 `git push` / `crawl extract|nlr-batch|update|…` / 毁 data;force-push main 与 `reset --hard` 永拦;`npm run agent:selftest` 12 项全绿(含无回执 push 拦、有回执放行、只读放行);`.claude/settings.json` SessionStart+PreToolUse;AGENTS.md 首节强制 preflight。
- confidence:high(可回放 selftest + commit 文件集;与 agent-on kit/guard「不赌读没读」同族但项目本地)
- claim:高风险项目在**本仓**应有 L-进场机械回执(真相源内容 hash + 硬约束注入 + 时效);对 push/批处理/毁数据等动作 fail-closed,不依赖模型自觉打开 AGENTS.md;与全局跨仓 git-guard 互补,不互相替代。
- suggested_landing:kit/guard/README 或新 kit/guard/session-receipt 模式说明 + playbook 进场层 L-进场 deferred 条可引用项目实证;AGENTS-skeleton 可选「高风险域 preflight」一行
- rollback:revert 落地 commit
- trace:IPONews scripts/guard/*; agent:selftest; 9f1514a
- 状态:landed@同批(第十一·卡4:kit/guard README L-进场会话回执模式 + AGENTS-skeleton 可选 preflight 行)

### modular-prompt-orphan-must-fail(模块化 prompt 的 know-how 必须挂载或测试红)
- source:IPONews @ 9f1514a | pin v0.4.0
- evidence:`prompts/{prospectus,allotment}/manifest.json` 拼装;`listOrphanKnowHow` 扫 `know-how/**/*.md`,未进 manifest 且非 README/`_*.md`/`*.draft.md` 则 orphan;`crawler/test/prompts.test.ts`「no orphan know-how」断言空数组;prompts README 写明挂载纪律。
- confidence:medium(单项目但失败模式普适:写了规则文件却静默不进模型)
- claim:当 system prompt 由多文件拼装时,**磁盘上存在却未列入装配表的规则文件必须使测试/门禁失败**(或显式草稿命名豁免);禁止「写了 know-how 以为生效、模型从未读到」的静默失效。
- suggested_landing:playbook 配置即代码/prompt 拼装纪律半句 + kit 若有 prompt 模板则加 orphan 自检示意;或 bench 案例 silent-unwired-config
- rollback:revert 落地 commit
- trace:IPONews crawler/src/prompts.ts listOrphanKnowHow; test/prompts.test.ts; 9f1514a
- 状态:pending
