# intake — Dartify @ 2026-08-19（单卡投递，非完整结账）

> 形态说明：这不是一次「agent-on 结账」的全量批次，是 DTCG 复验会话现场撞坑后的**单卡投递**。
> 投递会话：agent-on worktree `claude/dtcg-post-landing-revalidation-e105cd`（用户转述 Dartify 现场 + 本会话回 Dartify 仓自取证）。
> 落盘即止——本文件未 commit，git 动作归 agent-on 消化会话（intake/README 第 5 条）。

---

### local-catchup-collides-with-server-update-branch（本地追平与服务端 update-branch 撞车，规矩在但没管住功能会话）

- **source**：Dartify @ `642d5bf` | pin v0.9.1 (8f309da)
- **evidence**：Dartify `fix/token-mirror-gate-holes`（PR #204）2026-08-19 晚 31 分钟内的四笔提交，`git log --graph` 可直接回放分叉：

  | SHA | 时间 | committer | subject |
  |---|---|---|---|
  | `8a8d3f1` | 21:27:05 | GitHub | Merge branch 'main' into fix/token-mirror-gate-holes（服务端 update-branch #1） |
  | `bbf2274` | 21:56:34 | Dartify Contributors | Merge remote-tracking branch 'origin/main' into …（**本地追平，违规动作**） |
  | `c371708` | 21:57:08 | GitHub | Merge branch 'main' into …（服务端 update-branch #2，距上一笔 34 秒） |
  | `642d5bf` | 21:58:24 | Dartify Contributors | Merge remote-tracking branch 'origin/fix/…' into …（**push 被拒后的收拾**） |

  回放命令：`cd ~/Projects/Dartify && git log --graph --oneline -8 fix/token-mirror-gate-holes`
  对照组（说明服务端路径本身干净）：本仓与 Dartify 近三天所有 `Merge branch 'main' into <feature>` 提交 committer 均为 GitHub，唯独 `bbf2274` 是本地。

- **confidence**：low（本项目见过一次；但底层规则已 canonical 且有 2026-08-16/17 源流，本卡新增的只是「索引方式 + 拦截点」）
- **claim**：**已开 PR 的分支，任何让它吃进 base 的操作只走服务端 update-branch；本地不得 merge / rebase base 之后 push。** 判据按物理动作，不按意图——"为了合而追平"和"为了复验而拿到 main 的内容"是同一个动作，同一条禁令。
- **suggested_landing**：
  1. `playbook/multi-contributor-protocol.md` §三½.6 第 114 行——把「追平一律走服务端 update-branch API」改写成物理动作表述，并显式点名**功能会话也受此约束**（现行文字挂在「值守合并调度」小节里，功能会话读起来像别人的规矩）
  2. `kit/babysit/CONTRIBUTING-CLAUSE.md`——功能会话侧自检清单补一行，让约束出现在动作方读得到的地方
  3. `kit/worktree-control-plane.md` §Git hook（第 91 行起已有 shared `core.hooksPath` 装 pre-push 的机制）——加一条 pre-push 检查：待推提交里出现以 `origin/<default>` 为第二 parent 的本地 merge commit（committer ≠ GitHub）→ 拦，报错文案直接给出 `gh api -X PUT repos/<owner>/<repo>/pulls/<N>/update-branch` 的完整命令（出口面：报错文案即工单，§三½.4）
- **rollback**：revert 落地 commit；hook 侧沿用既有 `--no-verify` 逃生口（worktree-control-plane 已声明其边界，不新增逃生口）
- **trace**：用户 2026-08-19 转述 Dartify DTCG 复验现场；本会话回 `~/Projects/Dartify` 自取证得上表四锚点。与 `multi-contributor-protocol.md:65` 的审计发现（`merge --no-ff` 被 7/7 squash、issue 建制 24 天 0 使用）同族——digest `paper-mechanism-rots-silently`，本条是该 slug 的又一实例，消化时建议合并考虑：**纸面规矩的失效常见于"索引在意图词上"而非"缺规矩"**。
- **状态**： landed@同批（playbook/multi-contributor-protocol.md §三½.8 + kit/babysit/CONTRIBUTING-CLAUSE.md 第 2/4 条）。pre-push 机械闸原 deferred（`cli/src/**` 属硬停第 1 类），2026-09-26 CLI 轨 landed@同批（cli/src/prepush.rs + worktree_hooks.rs；两处落点的「待实现」改成实况；按出口可达性，有冲突的 merge 与没开 PR 的分支放行）。原标 `landed@v0.22.0` 是 09-21 未提交消化预写的，v0.22.0 实际不含这些改动
