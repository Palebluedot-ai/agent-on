# Snapshot — 2026-07-08 v0.2 收官（融合全程对账单）

> 职责边界：记录 2026-07-07~08 两天融合工程的最终状态与逐仓对账。冷启动恢复读本份 + CHARTER + CHANGELOG 即可满血。

## 一、状态一句话

agent-on v0.2 全量落地并已推 GitHub（tag v0.1.0/v0.2.0）：五块资产、三入口两口令、S/M/L 档位路由、迭代闭环六站机制、Bench 案例集 17 卡、三前身仓归档、全局 CLAUDE.md 路由。批一~批四完成；批五（dogfood）等第一个真实项目。

## 二、逐仓对账单（用户确认件：都打包进来了吗？）

| 源 | 提取进 agent-on 的精髓 | 明确不进（死亡名单） | 源仓处置 |
|---|---|---|---|
| **一代** agent-orchestration-playbook（2026-04~05） | 四张卡 JSON Schema（含 fencing 锁语义）→ kit/schemas/；DoD 四条+最小卡集 → merge-checklist；「默认隔离/单写者」理论 → playbook；audit-lint 状态机校验 → ledger/；新场景问卷 → boot/；commit 分层 → kit/；失败降级结构、「上下文边界优先/单 agent 优先」「不许感觉更好」→ meta-principles；初心引言 → playbook 扉页；翻车标本 → 案例 16 | 自建运行时全部蓝图（Router/Lane/Policy 注入/phase0 步骤）、多平台消息路由（Discord/channel-repo）、全部产品 roadmap、自动生成 reports | 已归档（README 标头已推） |
| **二代** communication-governance-playbook（2026-05~06，含 non-drift 起点——GitHub 上实为同一仓，改名重定向） | 真相源三层仲裁 → truth-hierarchy；三态转移+6 理由码+fail-closed 双步解锁 → phase-gates；Session Handshake → boot/session-handshake；Truth Table → bench/truth-table-template；self-dogfooding+「治理进 Git」→ meta-principles；翻车标本 → 案例 17 | 锁口令逐轮复述体系（仅保留 fail-closed 思想）、MRD/PRD 实例、选型记录、Hermes 会话 JSON | 已归档（标头已推；本地 non-drift 文件夹=改名前旧 checkout，其标头提交仅存本地） |
| **二代半** project-kickoff-os（=本仓 5 月内容） | 愿景与三阶段路线 → CHARTER；文档纪律六条 → AGENTS.md；PRD/需求澄清包/milestone 三模板 → kit/ | —（skill 拆解图存 legacy/，v0.3 skill 化决策时的输入） | 收编入 legacy/kickoff-os/ |
| **支线** evolving-memory-system（活仓，只挖协议不动仓） | Soul Memory L0-L4 → memory-layering；ABDC 四步法+四模板 → abdc-decision + kit/abdc/；修正闭环三态 → correction-loop；run 台账 jsonl 规范 → run-card-logging；memory_card 丰富枚举并入 schema；「下游 pin 只映射不复制」→ truth-hierarchy §4 + lock 机制 | 其记忆系统自身运维件（OpenViking/同步补偿/自建 runner/capture 管线） | 不动（活项目，gbrain 内容源） |
| **三代** Euan-Flutter 仓内 agent-on/ | 八篇方法论 + 机制七篇 + kit 十件 + run-log 数据 + loop-notes（→案例 01-15）**内容已全量复制进本仓** | — | 内容已进；文件夹本身待 v0.3 首结账时倒仓+降级为纯采集三件套（清双头，非补内容） |
| **旁支** skill-asset-governance | 暂未挖（registry/生命周期/冲突策略） | — | 原样保留；若 v0.3 决定走 skill 形态再回来 |
| **外部** agents-zone-skillset（lipingtababa） | 仅作相关工作引用（README 链接） | 不融合（他人资产） | 不动 |

## 三、本次工程的自举实证（方法论用自己造了自己）

- 融合裁决 = 三个深读代理 + 全景扫描（fusion-map）
- 迭代闭环设计 = ABDC 走了一遍（三镜头提案 → 对抗评审 → 合成）
- 批二/批三/档位路由 = 每批对抗审查收口，抓到并修掉：代际标错 9 处、audit-lint null bug、5 卡虚指清单（倒逼 sop 真建出集成清单——回路第一次真实运转）、settlement 对 S 档空转
- 完成幻觉照妖：交接快照声称存在的 security-playbook.md 实不存在，已入 memory 防再引用

## 四、遗留清单（诚实）

1. **批五未跑**：需要真项目。入口 = 存量项目说「读 boot/adopt.md 接管」或新项目走 BOOTSTRAP
2. **v0.3 验收**：Euan 仓内 agent-on/ 倒仓首结账 + 清双头
3. **CHARTER S 档锚点**已按用户档位拍板补入承诺 1（用户有否决权，未否决即生效）
4. Backlog 在 CHANGELOG：intake-lint / changelog-lint / 架构雷达移植 / 多协作者对接 / skill 化（消掉路径显性问题）
5. 一代仓「5200 行」口径已实测钉死：设计物 5251 行（md+schema+脚本），纯文档 4317 行

## 五、冷启动一句话

任何新会话：读本份 + [CHARTER.md](../CHARTER.md) + [CHANGELOG.md](../CHANGELOG.md)，即知 agent-on 是什么、有什么、缺什么；用法入口全在 [README.md](../README.md) 三入口表。
